<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Scanner barcode (mobile_scanner). Il pacchetto dichiara già questo
         permesso nel proprio manifest, ma esplicitarlo qui rende visibile
         che l'app usa la fotocamera senza dipendere da scelte interne di
         una libreria di terze parti. -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Il permesso CAMERA implica che l'app richieda l'hardware fotocamera
         e l'autofocus, e il Play Store escluderebbe i dispositivi che non
         li hanno. Qui sono opzionali: senza fotocamera restano comunque
         disponibili l'inserimento manuale e la scelta dell'immagine dalla
         galleria. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false"/>

    <!-- Notifiche di scadenza. POST_NOTIFICATIONS è richiesto da Android 13
         in poi; RECEIVE_BOOT_COMPLETED serve al boot receiver più sotto per
         riprogrammare i promemoria dopo un riavvio del dispositivo. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="mealbase"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Componenti di flutter_local_notifications. Dalla versione 16 il
             plugin dichiara nel proprio manifest solo POST_NOTIFICATIONS e
             VIBRATE: i receiver per le notifiche programmate vanno
             dichiarati dall'applicazione.

             Il primo riceve l'allarme dell'AlarmManager e costruisce la
             notifica: senza, zonedSchedule() registra l'allarme ma nessun
             componente lo riceve, quindi non compare nulla e l'app non
             segnala alcun errore. Il secondo riprogramma le notifiche dopo
             un riavvio o un aggiornamento, eventi che altrimenti azzerano
             tutti gli allarmi pendenti.

             Vanno dentro <application>: Android ammette <receiver> solo
             come figlio di questo elemento. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>

    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
